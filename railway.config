# Railway Deployment Configuration

# This file contains the configuration for deploying to Railway
# Railway automatically detects and deploys Node.js applications

# Environment Variables (set in Railway dashboard):
# MONGODB_URI - Your MongoDB Atlas connection string
# NODE_ENV - production

# Build Command
BUILD_COMMAND="npm run build"

# Start Command
START_COMMAND="node server.js"

# Root Directory
ROOT_DIRECTORY="."

# Install Command
INSTALL_COMMAND="npm install"

# For the API (if deploying separately):
# Build Command: cd api && npm install && npm run build
# Start Command: cd api && node server.js

# Port Configuration
PORT=3000

# Health Check
HEALTH_CHECK_PATH="/api/health"

# For dharmadkunwar.com.np domain setup:
# 1. Add your custom domain in Railway dashboard
# 2. Update DNS records as instructed by Railway
# 3. Configure SSL certificate